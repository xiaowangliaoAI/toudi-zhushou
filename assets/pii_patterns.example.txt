# 个人关键词清单（每行一个正则，忽略空行与 # 开头）
#
# 用法：cp pii_patterns.example.txt pii_patterns.txt 后把下面的占位换成自己的。
# ⚠️ pii_patterns.txt 本身**不要随包发出去**（它会列出你的真实信息）。
#
# 下面是**占位格式**，不是真实信息。把 <...> 换成你自己的内容再跑 pii_scan.sh。
# 注意别把真实值直接写在这个示例文件里 —— 它是随包分发的。

# 姓名 / 笔名
# <姓名>|<笔名>

# 手机 / 邮箱
# <手机号>|<邮箱>

# 所在城市（如果包里不该出现）
# <城市1>|<城市2>

# 学历 / 简历特征
# <学校名>|<专业名>|<简历里的独有数字>

# 投递过的公司名（列不全就只放特征词）
# <公司名1>|<公司名2>

# 本机账号名（一般会被通用规则 /Users/xxx 覆盖，不必单列）
# <本机用户名>
