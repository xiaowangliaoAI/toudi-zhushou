---
name: toudi-zhushou
description: 投递助手（toudi-zhushou）—— BOSS直聘岗位批量投递/打招呼的可复用工作流。读简历→搜岗位→逐条读JD判匹配→发招呼语→核对送达。含跨平台驱动层（macOS/Windows）、元素定位校准、匹配判定与红线规则、招呼语构造法、送达核对纪律、分享前的隐私自检与法律声明。仅限个人自用，禁止商业利用（见 LICENSE 与 DISCLAIMER.md）。当用户要求「帮我投BOSS直聘」「批量打招呼」「按简历筛岗位」「投递招聘岗位」时使用。触发词：投递助手、BOSS直聘、boss投递、批量打招呼、立即沟通、岗位投递、简历筛岗、招聘岗投递。
agent_created: true
version: 2.0.0
license: "PolyForm-Noncommercial-1.0.0（见 LICENSE，仅限非商业）"
verified_at: "2026-09-18"
note: "基于该日期的页面结构与风控行为验证；平台改版后需重新校准，发布越久越可能失效"
---

# 投递助手 · toudi-zhushou

> ⚠️ **使用前必读 `DISCLAIMER.md`（法律与风险声明），使用条件见 `LICENSE`（PolyForm Noncommercial 1.0.0，仅限非商业）。**
> 五条底线：
> **①仅限你本人投递简历使用；**
> **②你拿它干什么、后果都是你自己的事，与作者无关；**
> **③本工具不收集、不存储、不上传你的任何个人信息（零网络请求、零埋点、零数据库，登录由你自己的浏览器完成）；**
> **④🔴 存在封号风险，谨慎使用 —— 一旦使用，后续产生的相应责任由使用者自行承担，与本工具的著作权人无关；**
> **⑤🕐 工具会过期 —— 最后验证日期 2026-09-18，发布越久越不保证有效（见 `README.md`「时效性」一节）。**
> 这是一份**声明，不是护身符**——它不能把违规变合规，也不能免除使用者自己的责任。

> **再读 `references/portability.md`**——它说明了在一台陌生电脑上跑需要什么条件、哪些东西不能直接搬。分享给别人时，务必让对方先看那一份。

## 这套东西解决什么

把「看简历 → 搜岗位 → 读JD判匹配 → 发招呼语 → 核对送达」这条链路自动化，**但不做成无人值守的群发机器**。核心价值在**判断规则**（匹配怎么判、红线在哪），不在点击本身。

## 三层结构（决定了什么能复用）

| 层 | 内容 | 跨机器可复用性 |
|---|---|---|
| **判断层** | 匹配判定标准、红线清单、招呼语构造法、JD→经历映射 | ✅ **100% 可复用**，与方法论绑定，与机器无关 |
| **采集层** | 抓页面文本、筛选换批、会话列表核对、送达计数 | ✅ **100% 可复用**，纯浏览器+键盘操作，Mac/Win 都行 |
| **驱动层** | 点击/滚轮/输入/截图的落地实现 | ⚠️ **需分平台**，从本机搬过去必须换实现（见下） |
| **坐标层** | 按钮的固定像素坐标（每台机器、每种缩放都不一样） | ❌ **完全不可复用**，必须现场重新校准 |

**口径**：本机（这台 Mac）跑出来的**流程、规则、话术、判据**全部可搬；**写死的坐标、本机专属脚本路径、浏览器窗口位置**一律不可搬。

## 驱动层的两条路线（分享给别人时让 ta 选）

### 路线 A（推荐 · 真跨平台 · 无坐标依赖）

用 `agent-browser-core`（`agent-browser` CLI）驱动，元素 ref 定位：

- **复用登录态**：`--user-data-dir <独立profile目录>`，第一次让用户**手动登录**一次，之后复用。
- **挂载已开浏览器**（绕过复杂登录）：`--cdp <调试端口>`。
- **优点**：Mac/Win 同一套命令；基于元素 ref 而非坐标，**换分辨率/缩放不会失效**；`--json` 便于程序化。
- **⚠️ 风险**：这类自动化通道**可能被平台风控识别**。本机实测过 CDP 通道会触发「安全验证」；`--cdp`/独立 profile 是否被拦**必须先在目标账号上小样实测**（3–5 个岗位），确认不弹验证码再放量。
- **未实测的部分要如实说明**，不要替对方打保票。

### 路线 B（兜底 · 更像真人 · 需分平台）

用**系统级合成输入**驱动用户自己已登录的浏览器：

- macOS：CoreGraphics（`CGEventPost`），需在「系统设置 → 隐私与安全性 → 辅助功能」把调用方 App 加进去。
- Windows：`SendInput` / `mouse_event` + `keybd_event`（本 skill 的 `scripts/uidrv.py` 已内置双后端）。
- **优点**：对网页而言等同真人操作，无自动化通道指纹。
- **代价**：坐标要**现场校准**；且 macOS 与 Windows 是两套实现。

> 路线选择的判定：**先试 A**（改动小、跨平台）。A 若被风控拦，再上 B。

## 标准作业流程

1. **读简历、建底稿**：把简历拆成「6–8 段经历 + 可量化成绩 + 技能栈」，产出一份**素材库**（见 `references/greeting-playbook.md`）。
   ⚠️ **必须只用简历里真实存在的事实**。不许推断、不许美化、不许替对方补经历。
2. **确定筛选口径**（写进 `assets/config.json`）：目标岗位关键词、城市、薪资下限、**明确排除的岗位类型**。
3. **首次跑之前先小样实测**：只投 3–5 个，确认「按钮流程 + 送达可见」都通，再放量。
4. **逐条处理**：选中岗位 → 读 JD（用抓页面文本，别截图）→ 按 rubric 判匹配 → 匹配就发。
5. **换批**：一批读完改筛选条件（工作经验/工作区域/关键词），**不要硬滚**（合成滚轮被浏览器节流到约 5px/次）。
6. **核对**：每 N 条去会话列表核对一次，**只认列表时间戳，不认自己的操作流水**（见「计数纪律」）。
7. **收尾产出**：已发清单（公司/招聘者/岗位/薪资/匹配要点）+ 未发清单（含原因）+ 待办。

## 计数纪律（吃过亏的一条）

**自己数操作次数会数错。** 唯一可信的来源是**平台侧的记录**：

- BOSS 的凭证在「消息」页会话列表：每条新招呼会有 `[送达]` 标记 + 时间戳。
- **步骤**：进消息页 → **刷新页面** → `Cmd+A / Ctrl+A` → 复制 → 数时间戳。
- **必须刷新**：SPA 的会话列表是缓存的，用浏览器「前进/后退」回去看到的是旧数据，会把「已送达」误判成「发送失败」。
- **虚拟滚动陷阱**：会话列表只渲染最新约 40 条，更早的拿不到（滚轮/方向键/缩放都推不动）。**如果要完整历史，必须从一开始就分段收集**，别指望事后回读。

## 校准（路线 B 必做，路线 A 免）

坐标不可复用，每次换机器/换分辨率/改窗口都要重来：

**0. 先判断工具是否还活着。** 平台改版后整个定位层会静默失效。先跑一次小样（3–5 个岗位）+ `locate.py button` 自检；找不到按钮就说明页面变了，别硬跑。判断清单见 `README.md`「时效性」一节。

1. 截屏：macOS `screencapture -x out.png`；Windows 用 `scripts/uidrv.py shot`。
2. 量比例：`python scripts/locate.py info out.png` → 得到截图物理像素与逻辑点比例（Retina 通常 2:1，Windows 125% 缩放为 1:1 若进程已 DPI-aware）。
3. 定位控件：
   - 按钮：`python scripts/locate.py button out.png`（按颜色连通域找实心按钮，默认青色 `86,187,188`）。
   - 列表卡片：`python scripts/locate.py cards out.png <x> <y0> <y1> --width 330`
     —— `x` 取**列表卡片区域左边缘**，`--width` 给一个能覆盖卡片的带宽（**不要扫到右侧详情面板/左侧导航**）。
     它按「行白像素占比」找卡片，输出每张卡的 `click_y`。
     ⚠️ **`--merge` 别设大**：卡片间隙通常 8–20pt，默认 8 是对的；设成 40 会把整列卡片糊成一张（曾踩过）。
4. **交叉验证**：定位结果与已知可用坐标比对，偏差应 < 20pt；偏差过大说明比例或缩放假定错了。
   ——快速自检：卡片 `click_y` 之差的中位数应等于实际行距（同类列表一般恒定，例如 149pt），对不上就是扫描参数错了。
5. **不要目测**：把截图交给模型看时图会被缩放，肉眼估坐标误差可达 5% 以上，小按钮必然点空。

## 决定性规则（红线）

1. **不编造**。简历里没有的，不许写进招呼语、不许当作匹配依据。常见禁区：把有限的带教经历说成「带团队」、简历里没有的经历不许补、把自由顾问期包装成公司任职。
2. **硬门槛不符就不投**。JD 里写死且简历无依据的（语言等级、执业证书、第一学历、年龄区间、特定行业年限），投了也是浪费。
3. **岗位实质要对得上**。标题叫「招聘专员」但职责是供应商开发、资源拓展、销售招募、纯 BP 的，不算招聘岗。
4. **有节流**。批次之间留间隔、单次放量前先小样实测。**不做无上限的批量群发。**
5. **不可撤回的动作先确认**。发消息、投递属于不可撤回；首次使用、放量、以及任何「超出用户已授权范围」的动作，都要先问。
6. **不替用户说话**。招呼语的内容立场归用户，AI 只负责形式与匹配。

## 常见坑速查

| 现象 | 根因 | 处理 |
|---|---|---|
| 点击没反应 | 坐标落在按钮边缘（目测误差） | 用 `locate.py` 按像素定位 |
| `locate.py cards` 只扫出 1 张卡 | `--merge` 设太大（如 40pt），卡片间隙被跨过，整列糊成一块 | 用默认 `--merge 8` 重跑；输出里的「间隙中位数」应明显大于 merge |
| `locate.py cards` 一张都扫不出 | 扫描带宽落到了详情面板/导航区，或卡片底非纯白 | 把 `x`/`--width` 移到列表区内；卡片底偏灰时调低 `--ratio` |
| 滚轮滚不动 | 合成滚轮被节流（约 5px/次） | 改筛选换批，别硬滚 |
| 地址栏回车不提交 | 浏览器地址栏对合成 Enter 不响应 | 用 `Cmd/Ctrl+[ ]` 前进后退代替导航 |
| 快捷键没生效 | 目标窗口不在前台 | 每条命令开头先把目标应用激活 |
| 列表内容像是旧的 | SPA 缓存 / 复制到了隐藏容器 | 刷新页面；详情面板可信、列表内容交叉验证 |
| 会话列表拿不全 | 虚拟滚动只渲染约 40 条 | 从一开始分段收集 |
| 薪资数字复制后丢了 | 站点用自定义字体渲染数字 | 关键数字单独截图确认 |
| AppleScript `-10004` | 调用方 App 缺 `NSAppleEventsUsageDescription` | 改走路线 B；或换调用方 |

## 分享前必做：跑一遍隐私扫描

分工给别人之前，**先跑这个**，别靠肉眼：

```bash
bash scripts/pii_scan.sh .          # 扫本 skill 目录
bash scripts/pii_scan.sh <解压目录>  # 扫打包后的产物
```

它叠加两类规则：**通用格式**（手机号/邮箱/`/Users/xxx` 绝对路径/微信号）+ **你自己的关键词**（读 `assets/pii_patterns.txt`）。

- 第一次用：`cp assets/pii_patterns.example.txt assets/pii_patterns.txt`，再把里面的 `<...>` 换成自己的姓名/城市/学校/简历独有数字/投递过的公司名。
- ⚠️ **`pii_patterns.txt` 本身不要随包发出去**（它列着你的真实信息）。
- 命中不一定是泄漏：讲「不要包含绝对路径」这类**规则本身**可以留；出现真实姓名/手机/邮箱/本人城市/本人简历数字 → **必须改**。

**特别提醒**：最常漏的不是姓名电话，而是**你自己写的「举例」**。举例时顺手抄了自己的真实数字/城市/经历，是这类文档最典型的泄漏方式。

### ⚠️ 维护须知：别让声明变成假话

`README.md` 与 `DISCLAIMER.md` 里写了三句硬承诺：

> 零网络请求、零埋点、不接触账号密码。

**这三句是事实陈述，不是宣传语。** 只要给 `scripts/` 里任何一个文件加上：

- 任何网络请求（`requests` / `urllib` / `socket` / 上传日志 / 版本检查 / 统计上报）；
- 任何读取浏览器 Cookie、密钥串、凭证文件的行为；
- 任何写入用户数据到磁盘且不清理的行为；

**就必须同步修改那三句。** 不改 = 虚假陈述，比没有声明更糟。

改动后自查：

```bash
grep -rniE "requests|urllib|socket|urlopen|httpx|aiohttp|telemetry|report" scripts/
```

输出为空才配得上那句话。

### ⚠️ 维护须知：更新「最后验证日期」

`SKILL.md` 的 front-matter 有 `verified_at` 字段，`README.md` 和 `DISCLAIMER.md` 顶部各有一处日期。

**每次实际验证过一轮之后（小样跑通、`locate.py` 定位正常），把这三处日期改成当天。**
如果你改了页面对应的定位参数或判定规则，日期也一并更新。

**这不是形式主义**：日期是给使用者判断「这份东西还有没有效」的唯一依据。
日期一直是旧的，使用者就会拿一个可能已经失效的工具去试，那比不写日期更糟。

## 文件

- `README.md` —— **仓库落地页**：装法（一条命令 + 可粘给 AI 的提示词）、前置条件、未验证项
- `LICENSE` —— **PolyForm Noncommercial License 1.0.0**（SPDX: `PolyForm-Noncommercial-1.0.0`）：任何商业目的都不属于被许可的目的
- `DISCLAIMER.md` —— **法律与风险声明**：许可管得了什么/管不了什么、附加使用条件、免责与责任限制
- `.gitignore` —— **提交前必须存在**：挡住 `assets/pii_patterns.txt`、真实配置、素材库、清单、截图
- `references/portability.md` —— **跨平台可行性 + 分享前检查清单**（先看这个）
- `references/matching-rubric.md` —— 匹配判定标准、红线、未发原因分类
- `references/greeting-playbook.md` —— 素材库搭建法、招呼语构造法
- `references/compliance.md` —— 风控、节流、合规与账号风险边界
- `scripts/uidrv.py` —— 统一驱动 CLI（macOS / Windows 双后端，自动选择）
- `scripts/locate.py` —— 截图像素定位（按钮 / 列表卡片 / 比例换算）
- `scripts/pii_scan.sh` —— **分享前隐私自检**（通用格式 + 个人关键词）
- `assets/config.example.json` —— 用户配置模板（含「必须替换」的占位项）
- `assets/pii_patterns.example.txt` —— 个人关键词清单模板（填完另存为 `pii_patterns.txt`，**别随包发**）
